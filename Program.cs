
using DCTI;
using Okaimono.src;

namespace Okaimono
{

    internal class Program
    {
        static void Main(string[] args)
        {
            // Profile profile = new();
            // profile.CreateUser();
            //profile.ReadUser();
            
            Render render = new();
            
            
            // string[,] table = { { "1", "2", "3"}, {"5", "5", "5"}, {"6", "6", "6"}};
            

            // for (int row = 0; row < table.GetLength(0); row++){
            //     for (int col = 0; col < table.GetLength(1); col++)
            //         Console.Write($"[{table[row,col]} | {row}:{col}]");
                
            //     Console.WriteLine();
            // }
            

            //Backend backend = new();
            //backend.Donation();

        }
    }
}















   